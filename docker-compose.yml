services:
  db:
    image: postgres:16
    container_name: valmer_db
    environment:
      POSTGRES_DB: valmer_dev
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
    ports:
      - "5439:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
